import React, {useState, useEffect} from 'react';
import {Alert, View} from 'react-native';
import axios from 'axios';
export default function EmailCheck({setIsShow, Email}: any) {
  useEffect(() => {}, []);
}
